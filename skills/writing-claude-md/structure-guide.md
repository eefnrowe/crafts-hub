# CLAUDE.md 结构设计指南

> 来源：Stanford "Lost in the Middle"（arXiv:2307.03172）+ Anthropic Context Engineering + CodeIF-Bench（arXiv:2503.22688）+ Cursor Agent Best Practices + AGENTS.md Linux Foundation 标准

## U 形注意力曲线

LLM 对上下文的注意力呈 U 形分布：

```
注意力
  ▲
  │ ████                                    ████
  │ ████                                    ████
  │ ████                                    ████
  │ ████         ████                       ████
  │ ████         ████         ████          ████
  │ ████         ████         ████          ████
  └──────────────────────────────────────────────► 位置
    首位效应         中间低谷         近因效应
   (最高)           (~30%)          (高)
```

**设计含义**：CLAUDE.md 每轮作为系统提示重新注入，固定在上下文窗口最前端，持续享受首位效应。最重要的规则放在文件开头。文件末尾的红线回顾通过重复强化关键规则起效，而非近因效应（近因效应的受益者是最新用户消息，不是文件末尾）。

## 推荐结构

每个 section 标注 **必选/条件选** 和 **最低行数**。LLM 严格按此骨架逐段填充，不得跳过必选 section。

```markdown
# [项目名称]                                         ← 必选，~1 行

> [项目身份：这是什么 + 核心技术栈 + 一句话核心流程]    ← 必选，1-2 行

## 适用范围                                          ← 必选，≥3 行
| 范围 | 说明 |
|------|------|
| 生效 | ... |
| 排除 | ... |

## 命令                                              ← 必选，≥5 行
| 操作 | 命令 |
|------|------|
| 安装 | ... |
| 测试 | ... |
| lint | ... |
| ...  | ... |

## 核心架构规则                                      ← 必选，≥15 行
[架构图或分层图]
[约束表：规则 | 说明 | ✅ | ❌]
[每个核心规则配代码示例，格式见下方"代码示例格式"]

## 通用类库依赖                                      ← 条件选（Q5 有输入时），≥5 行
| 库名 | 用途 | 关键约定 |
|------|------|---------|
| ...  | ...  | ...     |

## 代码风格                                         ← 必选，≥10 行
[命名约定表格]
[导入顺序 / 格式规则]
[至少 1 个 ✅/❌ 示例]

## 测试                                             ← 必选，≥8 行
[测试框架 + 运行方式]
[测试命名约定]
[测试模式要求（如隔离、mock 边界）]

## 维护义务                                         ← 条件选（Q9 提及时），≥5 行
[变更时必须同步的目标表]

## 配置层级                                         ← 可选，≥3 行
[配置优先级表格]

---

**红线：** 规则1 · 规则2 · 规则3                      ← 必选，1-2 行
```

### 最低行数预算

| Section | 类型 | 最低行数 | 说明 |
|---------|------|---------|------|
| 标题+项目身份 | 必选 | 3 | 标题 + 引用块 |
| 适用范围 | 必选 | 3 | 至少 2 行表格 |
| 命令 | 必选 | 5 | 至少 3 行命令表格 |
| 核心架构规则 | 必选 | 15 | 约束表 + 至少 1 个 ✅/❌ 示例 |
| 通用类库依赖 | 条件选 | 5 | Q5 激活时 |
| 代码风格 | 必选 | 10 | 命名表 + 至少 1 个示例 |
| 测试 | 必选 | 8 | 框架 + 命名 + 模式 |
| 维护义务 | 条件选 | 5 | Q9 提及时 |
| 配置层级 | 可选 | 3 | 有配置优先级时 |
| 红线 | 必选 | 2 | 一句话复述 |
| **必选合计** | | **~46** | 仅必选 section 最低行数 |
| **含条件选** | | **~56** | 典型项目 |

### 行数扩展规则

必选 section 填充后总行数 < 150 行时，按优先级依次扩展：

1. **扩展代码示例**：核心架构规则中每个规则增加第二个 ✅/❌ 示例（展示边界情况）
2. **扩展测试 section**：增加测试模式说明（单元/集成边界、mock 策略、fixture 约定）
3. **扩展代码风格**：增加导入顺序规则、文件命名约定
4. **添加常见错误**：代码风格 section 末尾增加"常见违反"子段
5. **添加架构图**：核心架构规则前增加文本形式的分层/模块关系图

**禁止**通过以下方式凑行数：重复 README 内容、写入工具链已覆盖的规则、添加与项目无关的通用建议。

## 行数控制

- **下限**：150 行（必选 section 最低预算 ~46 行 + 扩展至 150 行）
- **上限**：200 行（Anthropic 官方建议）
- **ETH 研究警示**：自动生成文件比没有文件更差——每行都要经得起"删掉它会导致 Claude 犯错吗？"的检验
- **注意力预算**：冗余内容增加推理成本并降低成功率（ETH 研究证实），每行都应通过"删掉会导致 agent 犯错吗？"检验
- **行数不足时**：按"行数扩展规则"优先级依次扩展，不得跳过直接提交

## 措辞规则

| 场景 | 用词 | 示例 |
|------|------|------|
| 硬约束（安全/架构） | `禁止` / `必须` / `唯一` / `NEVER` / `MUST` | "禁止 `async def`" |
| 软约束（最佳实践） | `推荐` / `建议` / `优先` / `recommended` | "推荐构造器注入" |
| 反直觉约束 | `必须保留并强调` + 解释为什么 | "全同步架构。唯一豁免：app.py lifespan" |
| 代码示例 | `✅` / `❌` 双向约束 | 见下方 |

## 代码示例格式

每个核心强制规则配一个正反例：

````markdown
### 规则名称

[一句话说明规则]

```python
# ✅ 正确做法
@provide
def skill_repository(self) -> SkillRepository:
    return InMemorySkillRepository()

# ❌ 禁止
service = SkillRouterService(...)  # DI-001 违规
```
````

## 引用文件模式

**Coding Agents 研究（arXiv:2603.20432）：** Coding agent 具备文件结构的隐式知识，能通过目录命名和层级组织自行导航定位信息。因此引用文件路径优于复制内容——agent 能高效读取文件，无需在 CLAUDE.md 中展开。

**Cursor 官方建议：** "参考文件而非复制内容"。当一个模式已有代码示例，用引用替代复制：

| 写法 | 效果 |
|------|------|
| `See Button.tsx for canonical component structure` | agent 自行读取文件，不消耗 CLAUDE.md 行数 |
| 复制 200 行 Button.tsx 到 CLAUDE.md | 占用大量行数，且容易过时 |
| `架构分层详见 README.md § Architecture` | 避免与 README 冗余 |

**适用场景：**
- 项目有 canonical example 文件（如 `examples/` 目录）
- 规则在其他文档中已有详细说明
- 代码模式复杂但已有标准实现

## 模块化与 import 机制

当 CLAUDE.md 超过 150 行或项目为 monorepo 时，考虑拆分：

### Anthropic Claude Code：`@path` 语法

```markdown
# CLAUDE.md（主文件）
@./docs/conventions.md
@./docs/testing.md
```

- 支持相对路径和绝对路径
- 最大递归深度 5 层
- 子目录的 CLAUDE.md 在读取该目录文件时 JIT 加载

### monorepo 嵌套文件策略

| 场景 | 做法 |
|------|------|
| Monorepo（多服务） | 根目录 CLAUDE.md 写共享规则，各服务目录写特有规则 |
| 单体项目（>150 行） | import 拆分：主文件写核心规则，`@./docs/` 引用详细规范 |
| 微前端 | 每个子应用目录独立 CLAUDE.md，根目录写跨应用共享规则 |

## 多工具互操作

主流 AI 编码工具的指令文件正在趋同：

| 工具 | 文件名 | 发现机制 | import 语法 |
|------|--------|---------|------------|
| Claude Code | `CLAUDE.md` | 向上递归 + JIT | `@path` |
| Gemini CLI | `GEMINI.md` | 全局/工作区/JIT | `@file.md` |
| OpenAI Codex | `AGENTS.md` | 根到 cwd 拼接 | 无 |
| Cursor | `.cursor/rules/*.md` | 文件级 + Auto | 无 |
| Augment | `CLAUDE.md` 或 `AGENTS.md` | 向上递归 | 无 |

**跨工具复用建议：**

1. **以 AGENTS.md 为基础文件**：已由 Linux Foundation 标准化，被 OpenAI Codex、Google Jules、Cursor、Augment 支持
2. **根目录 symlink**：`CLAUDE.md → AGENTS.md`，让 Claude Code 和其他工具共用一份
3. **Gemini CLI 配置**：在 `settings.json` 中设置 `"context": { "fileName": ["AGENTS.md", "CLAUDE.md"] }`
4. **内容兼容性**：使用纯 Markdown，避免工具特有语法（如 YAML frontmatter 仅在 Cursor/Augment 中有效）

## 指令遗忘缓解

> 来源：CodeIF-Bench（arXiv:2503.22688）— 多轮交互中指令遵循面临挑战

**问题**：多轮交互中，agent 可能遗忘之前遵循的规则，尤其是中间区域的规则。

**缓解策略：**

1. **红线回顾**（已有）：文件末尾一句话复述最关键规则，通过重复强化加深遵循
2. **控制文件长度**：越短越不容易被遗忘
3. **避免信息堆砌**：ETH 研究证实，不必要的指令让 agent 认为任务更难，运行更多步骤

## 信息密度优化

| 方式 | 替代 | 原因 |
|------|------|------|
| 表格 | 散文段落 | 同信息量 token 更少 |
| 代码示例 | 3 段文字描述 | Anthropic + Cursor 研究证明一个示例 > 三段描述 |
| 项目符号 | 长段落 | 检索更准确 |
| `✅/❌` | 纯文字 | 视觉区分度高 |
| 红线回顾 | 无 | 重复强化关键规则，一句话复述 |
| 引用文件路径 | 复制代码内容 | 节省行数，避免过时 |

## 质量验证清单

生成 CLAUDE.md 后逐项检查：

1. **行数** 150-200 行？
2. **首位效应**：开头是否有项目身份 + 命令 + 核心架构？
3. **重复强化**：结尾是否有红线回顾？
4. **代码示例**：每个核心强制规则是否有 ✅/❌ 示例？
5. **冗余**：是否有工具链已覆盖但仍写入的规则？
6. **冗余**：是否有 README/docs 已覆盖但仍写入的内容？
7. **最小充分性**：每行通过"删掉会导致 agent 犯错吗？"检验？
8. **互操作性**：如需跨工具使用，是否采用兼容格式？
9. **措辞**：硬约束是否用了绝对语言？
10. **信息密度**：是否有可以表格化的散文段落？
